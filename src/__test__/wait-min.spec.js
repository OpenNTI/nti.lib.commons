import waitMin from '../wait-min';

describe('wait-min tests', () => {
	it('Fulfills before min wait time, waits at least the min wait time and then resolves with result', (done) => {

	});


	it('Fulfills after min wait time, resolves with result', () => {

	});
});
