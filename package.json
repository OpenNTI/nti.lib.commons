{
  "name": "nti-commons",
  "version": "1.0.7",
  "author": "nextthought",
  "license": "UNLICENSED",
  "description": "Common utility javascript functions & classes.",
  "files": [
    "lib"
  ],
  "engines": {
    "node": ">=5.0.0",
    "npm": ">=3.3.0"
  },
  "scripts": {
    "test": "karma start --single-run --reporters dots,html,junit,coverage",
    "start": "make check",
    "prepublish": "not-in-install && make || in-install",
    "bump": "npm version",
    "postbump": "git push && git push --tags && npm publish"
  },
  "repository": {
    "type": "git",
    "url": "https://repos.nextthought.com/git/nti.lib.commons"
  },
  "publishConfig": {
    "registry": "https://npm.nextthought.com"
  },
  "dependencies": {
    "isempty": "0.0.1",
    "nti-util-logger": "^1.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.1.0",
    "babel-eslint": "^6.0.0",
    "babel-polyfill": "^6.1.0",
    "babel-preset-es2015": "^6.1.0",
    "babel-preset-stage-1": "^6.1.0",
    "eslint-config-nti-codestyle-js": "^1.1.0",
    "in-publish": "^2.0.0",
    "nti-unittesting-clientside": "^1.0.3"
  }
}
